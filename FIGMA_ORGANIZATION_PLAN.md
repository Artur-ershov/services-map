# План упорядочивания структуры Figma

## Текущая ситуация

У вас сейчас:
1. **Хаос с названиями и папками слоев в Figma** - разные форматы именования
2. **Таблица со списком сервисов** (отдельная от Figma)
3. **Данные в data.js** - сервисы с разными форматами `areaId`:
   - `area_10`, `area_4`, `area_3` (для B1-F3)
   - `Group 1201`, `Group 1202` (для B3-F1)

## Проблемы

1. **Несогласованность именования:**
   - Разные форматы ID слоев (`area_*` vs `Group *`)
   - Нет единой структуры папок
   - Сложно связать таблицу сервисов со слоями в Figma

2. **Связь данных:**
   - `areaId` в data.js должен соответствовать ID слоя в Figma
   - При экспорте SVG ID слоев сохраняются как атрибут `id`
   - Несоответствие приводит к проблемам с отображением на карте

## Решение

### 1. Стандартизация именования

#### Фреймы этажей:
**Формат:** `{building}-F{floor}`

Примеры:
- `B1-F3` - Корпус 1, 3 этаж
- `B3-F1` - Корпус 3, 1 этаж

#### Слои сервисов:
**Формат:** `service_{id}_{name}`

Примеры:
- `service_401_Переговорка-Альфа`
- `service_502_Вендинг`
- `service_503_Гардероб`

**Правила:**
- ID сервиса из data.js
- Название без пробелов (используйте дефисы)
- Убрать кавычки из названий

#### Структура папок:
```
📁 Building_B1
  📁 Floor_3
    📁 Services
      📄 service_401_Переговорка-Альфа
      📄 service_402_Переговорка-Север
      ...
  📁 Floor_4
    ...
📁 Building_B3
  📁 Floor_1
    📁 Services
      📄 service_501_Кофе-точка
      ...
```

### 2. Связь с таблицей сервисов

#### Структура таблицы (рекомендуемая):

| ID | Название | Корпус | Этаж | ID слоя в Figma | Путь в Figma |
|---|---|---|---|---|---|
| 401 | Переговорка «Альфа» | B1 | 3 | service_401_Переговорка-Альфа | Building_B1/Floor_3/Services |
| 402 | Переговорка «Север» | B1 | 3 | service_402_Переговорка-Север | Building_B1/Floor_3/Services |
| ... | ... | ... | ... | ... | ... |

#### Связь с data.js:

В `data.js` поле `areaId` должно совпадать с **ID слоя в Figma**:

```javascript
{
    id: 401,
    name: 'Переговорка «Альфа»',
    building: 'B1',
    floor: 3,
    areaId: 'service_401_Переговорка-Альфа', // ← Должно совпадать с ID слоя в Figma
    // ...
}
```

### 3. План действий

#### Шаг 1: Анализ текущей структуры
- ✅ Создан `scripts/analyze-figma-structure.js` - анализ структуры
- ✅ Создан `scripts/create-service-mapping.js` - создание маппинга

#### Шаг 2: Создание маппинга
Используйте скрипты для создания маппинга между:
- Таблицей сервисов
- Слоями в Figma
- Данными в data.js

#### Шаг 3: Переименование в Figma
1. Создайте структуру папок (Building_X / Floor_Y / Services)
2. Переименуйте слои согласно формату `service_{id}_{name}`
3. Убедитесь, что ID слоя = ID из таблицы

#### Шаг 4: Обновление data.js
1. Обновите `areaId` в data.js для соответствия новым ID слоев
2. Проверьте связь: areaId в data.js = ID слоя в Figma

#### Шаг 5: Экспорт и проверка
1. Экспортируйте SVG из Figma
2. Проверьте, что ID в SVG соответствуют areaId в data.js
3. Проверьте отображение на карте

## Инструменты

### Скрипты для работы:

1. **`scripts/analyze-figma-structure.js`**
   - Анализ текущей структуры
   - Рекомендации по именованию

2. **`scripts/create-service-mapping.js`**
   - Создание маппинга сервисов
   - Экспорт в JSON и CSV
   - Использование: `node scripts/create-service-mapping.js --csv`

3. **`scripts/create-naming-guide.js`**
   - Создание руководства по именованию
   - Документация форматов

### Файлы конфигурации:

- **`figma-config.js`** - конфигурация для связи этажей с Figma node IDs
- **`service-mapping.json`** - маппинг сервисов (будет создан скриптом)
- **`service-mapping.csv`** - маппинг в формате CSV (для таблиц)

## Вопросы для уточнения

1. **Где находится таблица сервисов?**
   - Google Sheets?
   - Excel?
   - Другая система?

2. **Какой формат удобнее для синхронизации?**
   - CSV?
   - JSON?
   - Прямая связь с Google Sheets API?

3. **Нужна ли автоматизация?**
   - Скрипт для автоматического переименования в Figma (требует Figma Plugin API)?
   - Или достаточно руководства для ручного переименования?

## Следующие шаги

1. **Создайте маппинг сервисов:**
   ```bash
   node scripts/create-service-mapping.js --csv
   ```
   Это создаст `service-mapping.csv` с рекомендуемыми ID слоев

2. **Используйте CSV для синхронизации:**
   - Откройте `service-mapping.csv` в Excel/Google Sheets
   - Добавьте столбец "ID слоя в Figma (текущий)" для сравнения
   - Заполните столбец "ID слоя в Figma (новый)" из рекомендаций

3. **Приведите Figma в порядок:**
   - Создайте структуру папок
   - Переименуйте слои согласно маппингу
   - Убедитесь, что ID слоев соответствуют рекомендациям

4. **Обновите data.js:**
   - Обновите `areaId` в data.js для соответствия новым ID
   - Проверьте связь с SVG файлами

5. **Проверьте результат:**
   - Экспортируйте SVG из Figma
   - Проверьте отображение на карте
   - Убедитесь, что все сервисы связаны корректно





