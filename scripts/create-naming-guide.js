/**
 * Генератор руководства по именованию для Figma
 * Создает документ с правилами именования слоев и структуры
 */

const fs = require('fs');
const path = require('path');
const data = require('../data.js');

/**
 * Создает руководство по именованию
 */
function createNamingGuide() {
    const guide = `# Руководство по именованию для Figma

## Общие принципы

1. **Единообразие** - используйте одинаковый формат для всех элементов
2. **Связь с данными** - ID слоя должен соответствовать areaId в data.js
3. **Читаемость** - имена должны быть понятными и структурированными
4. **Совместимость** - имена должны работать в SVG экспорте

## Структура именования

### 1. Фреймы этажей

**Формат:** \`{building}-F{floor}\`

**Примеры:**
- \`B1-F3\` - Корпус 1, 3 этаж
- \`B3-F1\` - Корпус 3, 1 этаж

**Альтернативный формат (для отображения):**
- \`Корпус 1, 3 этаж\` (используйте в названии фрейма, но ID = B1-F3)

### 2. Слои/Группы сервисов

**Рекомендуемый формат:** \`service_{id}_{name}\`

**Где:**
- \`{id}\` - ID сервиса из data.js (например, 401, 402)
- \`{name}\` - название сервиса (транслитерация, без пробелов)

**Примеры:**
- \`service_401_Переговорка-Альфа\`
- \`service_502_Вендинг\`
- \`service_503_Гардероб\`

**Упрощенный формат (если нужно):**
- \`area_{id}\` - для простых случаев (например, \`area_401\`)

⚠️ **ВАЖНО:** ID в названии слоя должен совпадать с ID сервиса в data.js

### 3. Структура папок

#### Уровень 1: Корневые папки
\`\`\`
📁 Services          # Все сервисы (опционально)
📁 Building_B1       # Корпус 1
📁 Building_B2       # Корпус 2  
📁 Building_B3       # Корпус 3
\`\`\`

#### Уровень 2: Этажи внутри корпуса
\`\`\`
📁 Building_B1
  📁 Floor_1
  📁 Floor_2
  📁 Floor_3
  ...
\`\`\`

#### Уровень 3: Группировка на этаже (опционально)
\`\`\`
📁 Floor_3
  📁 Services        # Все сервисы этажа
  📁 Infrastructure  # Лестницы, лифты и т.д.
\`\`\`

## Маппинг: data.js ↔ Figma

### Связь между сервисами и слоями

В \`data.js\` каждый сервис имеет:
\`\`\`javascript
{
    id: 401,
    name: 'Переговорка «Альфа»',
    building: 'B1',
    floor: 3,
    areaId: 'service_401_Переговорка-Альфа', // Должно совпадать с ID слоя в Figma
    // ...
}
\`\`\`

**Правило:** \`areaId\` в data.js = ID слоя/группы в Figma

### Текущие несоответствия

Сейчас в data.js используются разные форматы:
- \`area_10\`, \`area_4\` (старый формат для B1-F3)
- \`Group 1201\`, \`Group 1202\` (формат для B3-F1)

**Рекомендация:** Приведите все к единому формату \`service_{id}_{name}\`

## Правила для SVG экспорта

1. **ID элементов** - сохраняются в SVG как атрибут \`id\`
2. **Имена групп** - становятся ID в SVG
3. **Пробелы** - заменяются на дефисы в SVG
4. **Спецсимволы** - избегайте использования специальных символов

## Примеры преобразования

### Пример 1: Переговорка «Альфа»

**В data.js:**
\`\`\`javascript
{
    id: 401,
    name: 'Переговорка «Альфа»',
    areaId: 'service_401_Переговорка-Альфа'
}
\`\`\`

**В Figma:**
- Имя слоя: \`Переговорка «Альфа»\` (для отображения)
- ID слоя: \`service_401_Переговорка-Альфа\` (для связи)
- Путь: \`Building_B1 / Floor_3 / Services\`

**В SVG:**
\`\`\`xml
<g id="service_401_Переговорка-Альфа">
  <!-- содержимое -->
</g>
\`\`\`

### Пример 2: Кофе-точка

**В data.js:**
\`\`\`javascript
{
    id: 501,
    name: 'Кофе-точка',
    areaId: 'service_501_Кофе-точка'
}
\`\`\`

**В Figma:**
- Имя слоя: \`Кофе-точка\`
- ID слоя: \`service_501_Кофе-точка\`
- Путь: \`Building_B3 / Floor_1 / Services\`

## Чеклист для упорядочивания

- [ ] Определить единый формат именования
- [ ] Переименовать все слои сервисов согласно формату
- [ ] Создать структуру папок (Building_X / Floor_Y)
- [ ] Обновить areaId в data.js для соответствия ID слоев
- [ ] Проверить экспорт SVG (ID должны сохраниться)
- [ ] Документировать структуру для команды

## Автоматизация

Для помощи в переименовании используйте:
- \`scripts/analyze-figma-structure.js\` - анализ текущей структуры
- \`scripts/create-mapping.js\` - создание маппинга между данными и Figma
`;

    const outputPath = path.join(__dirname, '..', 'FIGMA_NAMING_GUIDE.md');
    fs.writeFileSync(outputPath, guide, 'utf8');
    console.log(`✅ Руководство создано: ${outputPath}`);
    
    return outputPath;
}

// Если запущен напрямую
if (require.main === module) {
    createNamingGuide();
}

module.exports = { createNamingGuide };





